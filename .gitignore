# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.venv/
venv/

# Client drawings and generated outputs never go into git
samples/**/out/
out/
*.xlsx
!docs/**/*.xlsx

# The firm's template DXFs and the client drawings ARE committed, through Git LFS
# (see .gitattributes and docs/git-lfs.md). Only what the tool generates is ignored.
demo